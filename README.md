## Misc stuff used for installations

- `robo3t.desktop` is the file that registers Robo3T, for example, with the Ubuntu system. It contains the preferred pathname for self-installed programs (/usr/local/bin/)
